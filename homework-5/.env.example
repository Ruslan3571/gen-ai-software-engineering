# GitHub MCP (OAuth preferred — authenticate via Cursor MCP UI)
# PAT fallback: see docs/github-mcp-pat-fallback.md
# GITHUB_PAT=ghp_your_token_here

# Atlassian Jira MCP (OAuth preferred — authenticate via Cursor MCP UI)
# Legacy fallback: see docs/atlassian-mcp-fallback.md
# ATLASSIAN_API_TOKEN=
